apiVersion: v1
kind: Service
metadata:
  name: forward-deployed-ai-lab
spec:
  selector:
    app: forward-deployed-ai-lab
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
