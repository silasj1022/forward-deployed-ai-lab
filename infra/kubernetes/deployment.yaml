apiVersion: apps/v1
kind: Deployment
metadata:
  name: enterprise-agent-foundry
spec:
  replicas: 2
  selector:
    matchLabels:
      app: enterprise-agent-foundry
  template:
    metadata:
      labels:
        app: enterprise-agent-foundry
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: enterprise-agent-foundry
          image: ghcr.io/replace-with-owner/enterprise-agent-foundry:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: enterprise-agent-foundry-config
            - secretRef:
                name: enterprise-agent-foundry-secrets
                optional: true
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: runtime
              mountPath: /app/logs
            - name: runtime
              mountPath: /app/artifacts
      volumes:
        - name: runtime
          emptyDir: {}
